
export const metadata = { title: "TC-S World Mini", description: "Solar-native market with wallet" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="en"><body>{children}</body></html>)
}
